from sqlalchemy import create_engine
import pandas as pd

try:
    engine = create_engine("mysql+mysqlconnector://root:Wakacje101@localhost:3306/champions_league")


    # Testowe zapytanie
    df = pd.read_sql("SELECT * FROM teams", engine)
    print(df.head())

except Exception as e:
    print("❌ Błąd podczas połączenia lub zapytania:")
    print(e)
